# 微博真机阶段验收记录（2026-09-18）

当前状态：用户暂停真机验收，手机已取走，常驻控制服务已停止。尚未完成最终交付验收。
本次使用 HarmonyOS 6.1 真机，通过真实 stdio MCP → 常驻 Runtime → 设备边界执行。未执行发布、点赞、关注或评论。

## 已完成的实测

| 场景 | 结果 | 动作数 | 观察 P50 / P95 | 动作周期 P50 / P95 |
| --- | --- | --- | --- | --- |
| 发现→更多热搜→科技→话题结果→科技→热搜→发现→首页 | 10/10 轮通过 | 83（含 3 次准备动作） | 1265 / 1328 ms | 4390 / 5578 ms |
| 视频播放进度前进→上滑→不同视频 | 10/10 轮通过 | 10 | 1141 / 1219 ms | 4782 / 4907 ms |

动作周期包含动作前观察、执行与验证，不是底层输入注入耗时。当前结果尚不满足“响应极快”的目标。
视频验证的是可访问树中的播放进度及内容标识变化，不证明画面解码或音频正确。
报告时间戳采用 UTC；两项成功测试对应北京时间 2026-09-18 早晨。

## 修复与证据边界

- 热搜验收允许精确话题名称及其精确井号包装；兼容不同结果页共享的综合、实时、视频、图片标签。只对派发前拒绝的 stale_observation 刷新观察，绝不重放执行未知或已执行动作。
- 视频持续更新 Slider 导致滑动被误判为过期：新增仅供 back/home/swipe 使用的导航指纹，忽略数值 Slider 进度和指定时钟、电量节点的 text/originalText 变化。布局、焦点及其他属性变化仍拒绝动作；点击与输入保持完整指纹检查。
- 热搜成功记录来自导航指纹修复之前的版本，也早于验收脚本的最终整理。没有该轮脚本哈希，不补造；最新代码的热搜真机复测待恢复。
- 视频成功记录对应当前脚本 SHA256 a93e2a84581e5ba450eb64a26b308cbbbb3fd8658eb48dcbf259181fda6b74cd，使用导航指纹修复后的 Runtime。
- 保留三份失败记录：首次异常未捕获具体根因；随后明确记录过期观察拒绝；另一轮在完成六轮后因话题页匹配条件失败停止。失败后修正并重新执行，未将部分成功计入最终通过轮数。
- 随附 JSON 仅保存状态与耗时元数据，不含设备序列号、账户、输入内容、话题正文、原始 UI 树或截图。早期报告缺少结束时间和脚本哈希，按原样保留，不视为完整可重放证据。

## 待恢复验收

1. 搜索框聚焦与输入、准确查询结果及结果标签页切换。
2. 最终代码的热搜路径复测，视频扩展至设计要求的规模。
3. Codex、OpenCode、DeepSeek Harness 实际客户端验收。
4. 多应用 50/100 步任务、断连恢复、耐久与其他开发计划中的发布门槛。
5. 降低观察与动作周期延迟；补齐视觉定位、证据管理和任意任务规划能力。

本记录证明指定微博场景的阶段性能力，不等于完整 Artemis 替代品、任意跨应用任务或最终发布验收通过。

## 本地交付检查

暂停设备操作后重新运行：123 项回归测试通过（28.773 秒），pip check 通过，当前源码 wheel 构建成功。未进行干净环境安装验收。
